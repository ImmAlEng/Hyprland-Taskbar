[module]
name = "color-block"
version = 1
description = "Fills its allocated layout area with a configured color."
style = "style.css"

[generator]
entrypoint = "scripts/generate.py"

[dependencies]
commands = ["python3"]

[dependencies.arch]
packages = ["python"]
